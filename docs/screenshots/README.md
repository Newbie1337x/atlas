# Screenshots checklist

The main `README.md` (and its Spanish mirror) reference these files by name —
drop a replacement in here with the same filename and both READMEs update
automatically, no other edits needed.

| Filename | Screen | Status | Notes |
|---|---|---|---|
| `01-login.jpg` | `/auth/login` | ✅ done | Atlas wordmark + pulsing logo, Google/Facebook buttons |
| `02-home.jpg` | `/home` | ✅ done | Greeting + "Empezar entrenamiento" CTA + real routines (Lunes/Martes/Miércoles split) |
| `03-training.jpg` | `/training` | ✅ done | "Semanal" folder expanded with 3 routines |
| `04-session.jpg` | `/training/session/:id` | ✅ done | Mid-workout: KPI row, a checked-off set, rest timer banner pinned to the bottom |
| `05-profile.jpg` | `/profile` | ✅ done | The sub-nav list |
| `06-drag-drop.gif` or `.jpg` | `/training` | ⬜ optional | A routine card mid-drag between folders — a short screen recording converted to GIF sells this feature much better than a static shot could |

All five hero shots are cropped to just the app (no browser chrome/status
bar) at 1220×2417. Keep that convention for replacements — screenshot, then
crop off everything above the app's own header/toolbar.
